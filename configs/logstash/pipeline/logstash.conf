# Logstash Pipeline Configuration
# 基础配置用于日志收集

input {
  # TCP输入 - 接收应用程序日志
  tcp {
    port => 5000
    codec => json_lines
  }

  # Beats输入 - 接收来自Filebeat等的数据
  beats {
    port => 5044
  }
}

output {
  # 调试输出
  stdout {
    codec => rubydebug
  }
}